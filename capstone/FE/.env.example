# Change this to .env and it will run smoothly

NEXT_PUBLIC_API_URL=http://localhost:5000

# Server-only (never exposed to browser)
BACKEND_URL=http://localhost:5000


# If u run on Mac change the port to 8000 and or else it wont work